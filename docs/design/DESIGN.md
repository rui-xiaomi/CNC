# DESIGN.md — 兼容工作流索引（非规范正文）

> **UI 模式**：`spec-driven`（业务 Surface 布局 SSOT 在 PRD；全局视觉/交互不以本文件为准）
> **本文件性质**：兼容工作流的索引与指针。**禁止**在此复制色板、Token、控件映射或页面布局。

## 跟读指针

| 用途 | 文档 |
|---|---|
| 视觉与交互目标态 SSOT | [`docs/UI设计文档.md`](../UI设计文档.md) |
| 自研 Shell 与 WPF-UI 目标关系（当前有效 ADR） | [`docs/adr/0001-自研Shell与WPF-UI目标关系.md`](../adr/0001-自研Shell与WPF-UI目标关系.md) |

修改视觉规范时，**只修改** [`docs/UI设计文档.md`](../UI设计文档.md)；不要把规范正文写入本文件。

目标态与当前实现不一致时，必须结合上表 ADR 与 `src/` 源码判断，不得仅凭目标态文档直接重构现有 Shell。
