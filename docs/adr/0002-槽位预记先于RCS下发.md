# ADR-0002：自动派工先槽位预记，后 RCS 下发

- 状态：Accepted
- 日期：2026-08-04

## 背景

自动上下料当前先向 RCS 下发任务，再执行槽位预记。外部写入者在该窗口抢占槽位时，
实物任务可能已执行而本地账未锁定。事后取消不能消除 RCS 已接单或已启动的风险。

## 决策

自动派工由调度器预生成 taskId，并按以下顺序执行：

1. 使用 taskId 对源槽或目标槽做条件原子预记；
2. 仅预记成功后，使用同一 taskId 调用 RCS；
3. 下发失败或异常时补偿回滚预记；
4. 下发成功后保留预记，沿用现有完成落账、失败/取消回滚流程。

直接交接使用 `_expectedInbound` 作为进程内预登记，遵守相同先登记后下发顺序；
预登记先为 Pending，RCS 下发成功后才转为 Dispatched，目标工位不得提前消费 Pending 登记。
没有料架账的命名区搬运不创建虚假槽位记录。

## 结果

- 预记失败不再产生可执行 RCS 任务。
- 数据库条件更新继续承担并发仲裁，只有预记成功者可以下发。
- 数据库与 RCS 之间仍是补偿式事务，不宣称具备原子分布式提交能力。
- 手工 RCS 调用不传 taskId 时保持由服务生成，兼容现有入口。

## 后续加固（2026-08，不另立 ADR）

- **P0-6**：盘点回写与人工校正不得覆盖 `SLOT_STATE=Reserved`；外部写使用条件 UPDATE；UI 不得人工设置预记态。
- **Redo 补预记**：Redo / Redispatch / AutoRedo 发 RCS 前，已有预记幂等跳过，已回滚须再预记；预记失败拒发；本轮新锁且下发失败才回滚。上料有物料码只锁该件。命名区 / 换架 / grab / identify 无槽位账则跳过。手工无 taskId 首次下发不在此列。
- **落账未知不撤账**：COMPLETED + HasMat 未知：不 Confirm、不 Rollback、不放工位；读到符合才 Confirm，明确不符才 Rollback+Alarm。未绑加工位 COMPLETED 按方向直接落账。对账①b 与运行期同一 `SlotSettlement` 决策。
- 与软删路由（P0-5）、对账 fail-closed（P0-3）并列的安全门禁，见 `.scratch/final-acceptance/2026-08-05-cnc-loader-safety-hardening-acceptance.md`。
