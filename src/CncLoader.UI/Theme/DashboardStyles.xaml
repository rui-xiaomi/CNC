<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 监控看板：清晰描边 + 提亮面色，无 DropShadow（防糊字）。 -->

    <Geometry x:Key="DashIconOnline">M7,1.5 A5.5,5.5 0 1 1 6.99,1.5 M7,4.2 A2.8,2.8 0 1 1 6.99,4.2</Geometry>
    <Geometry x:Key="DashIconOk">M2.2,7.2 L5.5,10.5 L11.8,3.5 L10.4,2.2 L5.5,7.8 L3.5,5.9 Z</Geometry>
    <Geometry x:Key="DashIconNg">M3,3 H11 V11 H3 Z</Geometry>
    <Geometry x:Key="DashIconAlarm">M7,1.5 L13,12.5 H1 Z M6.3,5.2 H7.7 V8.2 H6.3 Z M6.3,9.2 H7.7 V10.6 H6.3 Z</Geometry>

    <DrawingBrush x:Key="DashCalibrationTicks" TileMode="Tile" Stretch="None"
                  Viewport="0,0,11,7" ViewportUnits="Absolute">
        <DrawingBrush.Drawing>
            <GeometryDrawing Brush="{StaticResource DashAccentBrush}">
                <GeometryDrawing.Geometry>
                    <RectangleGeometry Rect="0,0,1,7"/>
                </GeometryDrawing.Geometry>
            </GeometryDrawing>
        </DrawingBrush.Drawing>
    </DrawingBrush>

    <Style x:Key="DashPanel" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource DashSurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource DashBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="6"/>
    </Style>

    <Style x:Key="DashKpiCard" TargetType="Border" BasedOn="{StaticResource DashPanel}"/>

    <Style x:Key="DashDangerButton" TargetType="Button" BasedOn="{StaticResource DangerButton}">
        <Setter Property="Foreground" Value="{StaticResource DashAlarmBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource DashAlarmBrush}"/>
    </Style>

    <!-- 最近加工记录：原生 DataGrid，不继承 HandyControl（避免列宽被挤扁） -->
    <Style x:Key="DashRecordsHeader" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource DashSurface2Brush}"/>
        <Setter Property="Foreground" Value="{StaticResource FgMutedBrush}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="BorderBrush" Value="{StaticResource DashBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding" Value="0,0,0,0"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="8,0,8,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter VerticalAlignment="Center"/>
                            <Path Grid.Column="1" Margin="4,0,0,0" VerticalAlignment="Center"
                                  Fill="{StaticResource FgMutedBrush}" Stretch="Uniform" Width="8" Height="8"
                                  Data="M0,0 L8,0 L4,6 Z">
                                <Path.Style>
                                    <Style TargetType="Path">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Column.SortDirection, RelativeSource={RelativeSource TemplatedParent}}" Value="Ascending">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Setter Property="Data" Value="M4,0 L8,6 L0,6 Z"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Column.SortDirection, RelativeSource={RelativeSource TemplatedParent}}" Value="Descending">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Setter Property="Data" Value="M0,0 L8,0 L4,6 Z"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Path.Style>
                            </Path>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DashRecordsCell" TargetType="DataGridCell">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource FgBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}" Padding="8,0">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource DashSurface2Brush}"/>
                <Setter Property="Foreground" Value="{StaticResource FgBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DashRecordsRow" TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{StaticResource Border2Brush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <!-- 深色细滚动条：无上下箭头，弱对比滑块 -->
    <Style x:Key="DashScrollThumb" TargetType="Thumb">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="track" Background="{StaticResource ScrollThumbBrush}"
                            CornerRadius="3" Margin="1,0"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="track" Property="Background"
                                    Value="{StaticResource ScrollThumbHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="track" Property="Background"
                                    Value="{StaticResource DashAccentBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="DashVerticalScrollBarTemplate" TargetType="ScrollBar">
        <Grid Background="Transparent" Width="8">
            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="4" Margin="1,2"/>
            <Track x:Name="PART_Track" IsDirectionReversed="True" Margin="0,2">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Height="0"/>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource DashScrollThumb}" MinHeight="24"/>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Height="0"/>
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="DashHorizontalScrollBarTemplate" TargetType="ScrollBar">
        <Grid Background="Transparent" Height="8">
            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="4" Margin="2,1"/>
            <Track x:Name="PART_Track" Margin="2,0">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageLeftCommand" Opacity="0" Width="0"/>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource DashScrollThumb}" MinWidth="24"/>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageRightCommand" Opacity="0" Width="0"/>
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <Style x:Key="DashThinScrollBar" TargetType="ScrollBar">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Background" Value="Transparent"/>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="8"/>
                <Setter Property="MinWidth" Value="8"/>
                <Setter Property="Template" Value="{StaticResource DashVerticalScrollBarTemplate}"/>
            </Trigger>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="8"/>
                <Setter Property="MinHeight" Value="8"/>
                <Setter Property="Template" Value="{StaticResource DashHorizontalScrollBarTemplate}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DashRecordsGrid" TargetType="DataGrid">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="RowBackground" Value="Transparent"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource RowStripeBrush}"/>
        <Setter Property="HorizontalGridLinesBrush" Value="Transparent"/>
        <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="CanUserReorderColumns" Value="False"/>
        <Setter Property="CanUserResizeColumns" Value="False"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="CanUserSortColumns" Value="True"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="RowHeight" Value="32"/>
        <Setter Property="ColumnHeaderHeight" Value="30"/>
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource FgBrush}"/>
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource DashRecordsHeader}"/>
        <Setter Property="CellStyle" Value="{StaticResource DashRecordsCell}"/>
        <Setter Property="RowStyle" Value="{StaticResource DashRecordsRow}"/>
        <!-- 表头铺满滚动条上方缺口，避免右侧空一截 -->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGrid">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True"
                            Padding="{TemplateBinding Padding}">
                        <ScrollViewer x:Name="DG_ScrollViewer" Focusable="False">
                            <ScrollViewer.Template>
                                <ControlTemplate TargetType="ScrollViewer">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <Button Command="{x:Static DataGrid.SelectAllCommand}"
                                                Width="{Binding CellsPanelHorizontalOffset, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                Focusable="False"
                                                Visibility="{Binding HeadersVisibility, RelativeSource={RelativeSource AncestorType=DataGrid}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, ConverterParameter={x:Static DataGridHeadersVisibility.All}}"/>

                                        <DataGridColumnHeadersPresenter x:Name="PART_ColumnHeadersPresenter"
                                                                        Grid.Column="1"
                                                                        Visibility="{Binding HeadersVisibility, RelativeSource={RelativeSource AncestorType=DataGrid}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, ConverterParameter={x:Static DataGridHeadersVisibility.Column}}"/>

                                        <!-- 与表头同色，填满竖滚动条上方空隙（无滚动条时不占宽） -->
                                        <Border Grid.Row="0" Grid.Column="2"
                                                Background="{StaticResource DashSurface2Brush}"
                                                BorderBrush="{StaticResource DashBorderBrush}"
                                                BorderThickness="0,0,0,1"
                                                Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>

                                        <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                                Grid.Row="1" Grid.Column="1"
                                                                CanContentScroll="{TemplateBinding CanContentScroll}"/>

                                        <ScrollBar x:Name="PART_VerticalScrollBar"
                                                   Grid.Row="1" Grid.Column="2"
                                                   Orientation="Vertical"
                                                   Maximum="{TemplateBinding ScrollableHeight}"
                                                   ViewportSize="{TemplateBinding ViewportHeight}"
                                                   Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>

                                        <ScrollBar x:Name="PART_HorizontalScrollBar"
                                                   Grid.Row="2" Grid.Column="1"
                                                   Orientation="Horizontal"
                                                   Maximum="{TemplateBinding ScrollableWidth}"
                                                   ViewportSize="{TemplateBinding ViewportWidth}"
                                                   Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                   Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                                    </Grid>
                                </ControlTemplate>
                            </ScrollViewer.Template>
                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Resources>
            <Style TargetType="ScrollBar" BasedOn="{StaticResource DashThinScrollBar}"/>
        </Style.Resources>
    </Style>

</ResourceDictionary>
