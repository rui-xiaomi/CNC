# CONTEXT.md — 领域术语表

> **性质**：领域词汇的唯一权威来源（SSOT）。
> **只写词，不写需求。** 一个术语一行，一句话解释清楚"是什么、不是什么"。
> **规模**：十几到二十条足够。写超过三十条就会稀释，agent 反而抓不住重点。
> **判据**：只收录「不看解释会理解错」的词。通用编程术语（如"仓储""服务层"）不收。

---

## 写法示例

```
工单 = 一次可执行的加工任务，有唯一编号。不等于订单，一个订单可拆多个工单。
离线 = 与数据库连接中断超过 10 秒（心跳间隔 5 秒，连续 2 次失败）。
草稿 = 已录入但未提交的记录，不进入业务流转，不参与统计。
```

要点：
- 用 `=` 而不是句子，扫读快
- 有歧义风险的词必须写清"不等于什么"
- 涉及阈值的词直接写数字，别写"较长时间"

---

## 术语表

<!-- 从这里开始写，删掉上面的示例区 -->

术语 = 解释。

术语 = 解释。

---

## 已知的历史约定（不得"修正"）

> 记录那些看起来像 bug、实际是既定事实的东西。
> agent 最爱"顺手修正"这类拼写与命名，写在这里才能拦住。

- 例：`XXX_ID` 字段拼写错误为既定约定，与既有系统对齐，**禁止更正**。

---

## 与其他 SSOT 的分工

| 内容 | 写在哪 |
|---|---|
| 领域术语、历史约定 | **本文件** |
| 难以逆转的技术抉择 | `docs/adr/` |
| 视觉与通用交互规范 | `docs/design/DESIGN.md` |
| 页面规格、状态策略 | PRD Issue |
