# CONTEXT.md — 领域术语表

> **性质**：领域词汇的唯一权威来源（SSOT）。
> **只写词，不写需求。** 一个术语一行，一句话解释清楚"是什么、不是什么"。
> **规模**：十几到二十条足够。写超过三十条就会稀释，agent 反而抓不住重点。
> **判据**：只收录「不看解释会理解错」的词。通用编程术语（如"仓储""服务层"）不收。

---

## 术语表

智造云枢 LineOS = 线边 CNC 尺寸检测计量站的自动化上下料客户端（产线运行中枢）。不等于「AGV 客户端」或「机台上位机」。

计量站 = 现场检测服务对象；客户端为其调度上下料，不是通信对端。

通信边界 = 客户端只经 PLC（IP/TCP）与 RCS（HTTP 出站 + 本机回调入站）交互；不直连机台，不直连 AGV。

线体 = 可配置的产线顶层实体（WorkLine）。

工序 = 线体内一道检测/加工工序（Craftwork）。

机台 = 一台检测设备（通常含多个加工位）；表名历史拼写 EQUIMENT，见下方约定。

加工位 (Position) = 调度与位级 PLC 信号的最小单元；每机台通常 2 个，可并行上下料。不等于机台。避免使用：工位（仅口语）。

物料 (Material) = 槽位账目中的业务实体；电极码是其标识。避免使用：把「电极」抬成与物料并列的第二套主词。

料架 (Frame) = 独立主体的物料架，可多层多槽；可绑定到机台并带角色。

槽位 (Slot) = 料架内的物料孔位。避免使用：孔位（盘点/扫码口语）。

料架角色 (FrameRole) = 料架↔机台绑定上的角色（上料/下料/中转/NG）；同一料架可对多机台「一架两用」。

预记 (Reserved) = 下发 RCS 前的槽位占用意向（绑 taskId）；失败回滚。避免使用：预占（近义，不另立主词）。

落账 (Confirm) = 条件满足后把预记转为正式占用或清空。

位置映射 (LOCATION_MAP) = 逻辑位置 ↔ RCS 点位编码的配置真相源；缺失则拒发任务。禁止生成 FRAME-{id} 假码。

命名区域 = 位置映射中的逻辑区英文码（如 LOAD_AREA）；库与 RCS 用英文，UI 中文仅经显示转换，禁止中文回写库。

有料 (HasMat) = 加工位 PLC 读信号「当前是否有物料」。不等于已上料。读失败/异常为**未知**，不得当无料放行。

已上料 (LOADED) = RCS 上料完成且 PLC HasMat **确认 ON**。不等于「仅有料信号」或「读失败当无料」。

已下料 (UNLOADED) = RCS 下料完成且 PLC HasMat **确认 OFF**。

派工 (Dispatch) = 对加工位执行「选槽预记并下发 RCS」的自动循环；启动对账完成前不开自动派工。不等于换架/水位/对账。

受管派工 = 经路由解析/活动校验后的新外部 RCS 执行（自动/手动/Redo/换架/盘点等）；软删或未知配置 fail-closed 拒发。历史 Confirm/Rollback/Query 收口不另开新任务门禁。

启动对账 = 启动时对未完结 RCS 任务、预记/槽位账与 PLC 账实的三方收口；失败保持未开闸并按间隔重试，成功才开闸；首次对账不得阻塞后续 HostedService。避免使用：重启自检、任务恢复（过窄或过宽）。

RCS = 搬运/抓取调度系统；客户端的搬运对端。不等于 AGV。

AGV = 现场搬运执行体；经 RCS 调度，客户端不直连。

RCS 任务 = 一条搬运/抓取/盘点等任务的生命周期；表名 AGV_TASK 为历史名，不改主词。

报警粘滞 (Alarm latch) = 进入报警后须人工恢复；复核不过或不确认不写测试启动。不是可自动消除的瞬态。

---

## 已知的历史约定（不得"修正"）

- 表/字段历史拼写（如 `EQUIMENT`、`CARFTWORK_ID`）为既定 schema，禁止改名「纠正」。
- RCS 接口名 `excuteTask` 拼写错误为契约一部分，禁止改为 execute。
- 报警粘滞、启动对账完成前不开自动派工、缺 LOCATION_MAP 拒发、软删配置拒新派工、HasMat 未知 fail-closed：是安全设计，不是 bug。
- 软删用 `STATE='0'/'1'`（仅 `"0"` 为活动）；列表/查询与受管派工路由均须过滤；盘点/人工校正不得覆盖预记槽。

---

## 与其他 SSOT 的分工

| 内容 | 写在哪 |
|---|---|
| 领域术语、历史约定 | **本文件** |
| 难以逆转的技术抉择 | `docs/adr/` |
| 视觉与通用交互规范 | `docs/UI设计文档.md`（经 `docs/design/DESIGN.md` 索引） |
| 页面规格、状态策略 | PRD Issue |
