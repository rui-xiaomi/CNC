---
description: 
alwaysApply: true
---

@AGENTS.md

## Agent skills

### Issue 跟踪器

本地 Markdown：`.scratch/<feature-id>/`。见 `docs/agents/issue-tracker.md`。

### Triage 标签

默认五态：`needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`。见 `docs/agents/triage-labels.md`。

### Domain 领域文档

根目录 `CONTEXT.md` + `docs/adr/`；UI 视觉/交互 SSOT 为 `docs/UI设计文档.md`。见 `docs/agents/domain.md`。

### Repo Wiki（人类阅读）

`docs/repo-wiki/` 供人查阅；workflow skills 不读。见 `docs/agents/wiki.md`。
